{
  "_comment": "Config for scripts/readiness.py (see RELEASE_READINESS.md). Signals, available_models, weights and targets are read from here so the script stays byte-identical across projects. 'panels' starts empty: append one object per review panel (its participating models and NEW confirmed findings by severity) as panels are run, then re-run scripts/readiness.py.",
  "severity_weights": {"CRITICAL": 40, "HIGH": 10, "MEDIUM": 4, "LOW": 1, "NIT": 0.2},
  "available_models": ["opus", "sonnet", "haiku"],
  "tau": 2,
  "coverage_target": 0.90,
  "contract_coverage_estimate": 0.80,
  "known_open_defects": 0,
  "signals": {
    "property_tests": {"file": "tests/test_properties.py", "needle": "hypothesis"},
    "security": {"file": "tests/test_safety.py", "needles": ["traversal", "escape"]},
    "docs": ["LIMITATIONS.md", "SKILL.md", "CONTRIBUTING.md", "README.md"]
  },
  "panels": [
    {
      "id": "1",
      "commits": ["c3f5b5e"],
      "models": ["opus", "sonnet", "haiku"],
      "findings": {"HIGH": 1, "MEDIUM": 1, "LOW": 1},
      "note": "First panel. HIGH (consensus, 3 models): 7z backend broken against py7zr>=1.0 which removed SevenZipFile.read() (default suite blind, tests skip without py7zr). MEDIUM (singleton, reproduced): ZIP symlinks read as link-target bytes, not b'', violating the special-member-empty contract the tar backend honoured. LOW: _human_size emitted '1024.0P' past the petabyte unit."
    },
    {
      "id": "2",
      "commits": ["4ac5217"],
      "models": ["opus", "sonnet", "haiku"],
      "findings": {"MEDIUM": 2},
      "note": "MEDIUM (consensus, opus+sonnet): truncated gzip/xz leaked a raw EOFError/LZMAError from _SingleFileBackend._streamed_size past the exception hierarchy and crashed the CLI; now normalised to ArchiveReadError. MEDIUM (singleton haiku, reproduced; right-sized from HIGH): an over-NAME_MAX member name raised an uncaught OSError(ENAMETOOLONG) aborting extraction; now recorded in skipped_unsafe. Dismissed: haiku 'CRITICAL' single-file read() ignoring name (harmless single-member leniency; sonnet concurred below-threshold) and a cosmetic return-type NIT."
    }
  ]
}
